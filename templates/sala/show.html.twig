{% extends 'base.html.twig' %}

{% block title %}Sala{% endblock %}

{% block body %}
    {{ parent() }}
    <h1>Salas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sala.id }}</td>
            </tr>
            <tr>
                <th>Nombre sala</th>
                <td>{{ sala.nombreSala }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('sala_index') }}">Volver al listado</a>

    <a class="btn btn-outline-primary" href="{{ path('sala_edit', {'id': sala.id}) }}">editar</a>

    {{ include('sala/_delete_form.html.twig') }}
{% endblock %}
