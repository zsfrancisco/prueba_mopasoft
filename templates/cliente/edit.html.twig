{% extends 'base.html.twig' %}

{% block title %}Editar Cliente{% endblock %}

{% block body %}
    {{ parent() }}
    <h1>Editar Cliente</h1>

    {{ include('cliente/_form.html.twig', {'button_label': 'Update'}) }} <br>

    <a class="btn btn-primary" href="{{ path('cliente_index') }}">Volver al listado</a> <hr>

    {{ include('cliente/_delete_form.html.twig') }}
{% endblock %}
